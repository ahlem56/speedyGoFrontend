import { API_PATH } from "./global";

export const environment = {
    fakeData : false,
    apiUrl : 'assets/fake-data/',
    path : {
        user:{
            signin : 'user.signin.fake.json',
            signup : 'user.signup.fake.json',
        }
        
    }
}